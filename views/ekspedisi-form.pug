extends _bootstrap
//- extends layout

block body
  .container
    
    .page-header
      h1 Form Ekspedisi 
    
    form(action="/admin/ekspedisi/"+data.id method="post")
        <div class="form-group">
            <label for="exampleInputEmail1"> Nama Ekspedisi </label>
            input.form-control(type='text' placeholder='ekspedisi' name='nama' value=data.nama)
        </div>
        <button type="submit" class="btn btn-default">Simpan</button>

block scripts 
  script.
    //- console.log(datas);
    $(function () {
      $('[data-toggle="tooltip"]').tooltip()
    });